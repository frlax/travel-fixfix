{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cp .env.example .env && php artisan key:generate --force && composer install --no-dev --optimize-autoloader"
  },
  "deploy": {
    "startCommand": "php artisan serve --host=0.0.0.0 --port=${PORT}",
    "healthcheckPath": "/",
    "healthcheckTimeout": 60
  }
}